services:
  boltcard:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/bolt.db
